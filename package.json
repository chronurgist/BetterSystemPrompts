{
  "name": "agents-local-support",
  "private": true,
  "dependencies": {
    "@earendil-works/pi-coding-agent": "^0.80.3"
  },
  "devDependencies": {
    "vitest": "^3.1.3"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  }
}
